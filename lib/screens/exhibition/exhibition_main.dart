import 'package:flutter/material.dart';

class ExhibitionMain extends StatelessWidget {
  const ExhibitionMain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: [
            Text('메인')
          ],
        ),
      ),
    );
  }
}
